services:
  aesivia:
    build:
      context: .
    ports:
      - "8787:8787"
    volumes:
      - aesivia-local-state:/app/.wrangler
    environment:
      NODE_ENV: development

volumes:
  aesivia-local-state:
